# Contributing

Open an issue before a compatibility-affecting change. Keep commits focused and
lowercase conventional. Run `WOTEX_PATH_DEPS=1 mix check` and include tests for
changed behavior. Never add credentials, consumer-specific policy, or connection
ownership to this package.
