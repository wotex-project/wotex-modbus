# Instruction layout

- `CLAUDE.md` is the repository-wide contract.
- `.claude/rules/` contains path-sensitive implementation rules.
- `.claude/skills/` contains repeatable delivery workflows.

Select the narrowest matching rule and skill automatically.
